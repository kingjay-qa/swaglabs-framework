name: Playwright Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Validate test configuration
        run: |
          test -n "$BASE_URL" || (echo "BASE_URL secret is missing" >&2; exit 1)
          test -n "$STANDARD_USER" || (echo "STANDARD_USER secret is missing" >&2; exit 1)
          test -n "$LOCKED_USER" || (echo "LOCKED_USER secret is missing" >&2; exit 1)
          test -n "$STANDARD_PASS" || (echo "STANDARD_PASS secret is missing" >&2; exit 1)
        env:
          BASE_URL: ${{ secrets.BASE_URL }}
          STANDARD_USER: ${{ secrets.STANDARD_USER }}
          LOCKED_USER: ${{ secrets.LOCKED_USER }}
          STANDARD_PASS: ${{ secrets.STANDARD_PASS }}

      - name: Run Playwright Test Suite
        run: npx playwright test
        env:
          CI: true
          BASE_URL: ${{ secrets.BASE_URL }}
          STANDARD_USER: ${{ secrets.STANDARD_USER }}
          LOCKED_USER: ${{ secrets.LOCKED_USER }}
          STANDARD_PASS: ${{ secrets.STANDARD_PASS }}

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
